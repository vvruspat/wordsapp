diff --git a/node_modules/expo-audio/ios/AudioPlayer.swift b/node_modules/expo-audio/ios/AudioPlayer.swift
index 0742943..57b800d 100644
--- a/node_modules/expo-audio/ios/AudioPlayer.swift
+++ b/node_modules/expo-audio/ios/AudioPlayer.swift
@@ -267,6 +267,10 @@ public class AudioPlayer: SharedRef<AVPlayer>, Playable {
     removeQueuedLoopItems()
     ref.pause()
 
+    // Local files are already buffered; waiting for network stall minimization
+    // can leave short downloaded clips stuck before playback starts.
+    ref.automaticallyWaitsToMinimizeStalling = source.uri?.isFileURL != true
+
     if samplingEnabled {
       uninstallTap()
     }
diff --git a/node_modules/expo-audio/ios/AudioUtils.swift b/node_modules/expo-audio/ios/AudioUtils.swift
index 79c7c0b..01ebdb6 100644
--- a/node_modules/expo-audio/ios/AudioUtils.swift
+++ b/node_modules/expo-audio/ios/AudioUtils.swift
@@ -69,7 +69,10 @@ struct AudioUtils {
 
   static func createAVPlayer(from source: AudioSource?) -> AVPlayer {
     if let item = createAVPlayerItem(from: source) {
-      return AVQueuePlayer(items: [item])
+      let player = AVQueuePlayer(items: [item])
+      // Do not wait for network buffering when the source is a local file.
+      player.automaticallyWaitsToMinimizeStalling = source?.uri?.isFileURL != true
+      return player
     }
     return AVQueuePlayer()
   }
